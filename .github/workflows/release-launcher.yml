name: Publish launcher 0.5.9
on:
  push:
    branches: [main]
    paths: ['.release-inputs/v0.5.9/**']
permissions:
  contents: write
concurrency:
  group: launcher-release-0.5.9
  cancel-in-progress: false
jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Verify asset and publish prerelease
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .release-inputs/v0.5.9/publish.py
